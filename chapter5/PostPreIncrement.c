#include <stdio.h>

int main()
{
    int i = 0;
    
    printf("%d\n", i++); // Post Increment: Increments the value of i after its current value is used in the expression.
    printf("%d\n", ++i); // Pre Increment: Increments the value of i before its current value is used in the expression.

    return 0;
}
